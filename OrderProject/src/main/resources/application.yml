server:  servlet:    context-path: /weixinspring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/weixin?characterEncoding=utf-8
    username: root
    password: admin
    driver-class-name: com.mysql.jdbc.Driver
mybatis:
  type-aliases-package: com.example.order.entity
  mapper-locations:
  - classpath:/mapper/*.xml
logging:
  level:
    com.ysd.demo.dao: debug 

